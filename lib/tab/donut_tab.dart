import 'package:flutter/material.dart';

class donutTab extends StatelessWidget {
  const donutTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
    child: const 
    Center(child: Text('Donut tab'),)
    );
  }
}