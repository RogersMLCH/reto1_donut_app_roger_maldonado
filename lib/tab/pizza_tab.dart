import 'package:flutter/material.dart';

class pizzaTab extends StatelessWidget {
  const pizzaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
    child: const 
    Center(child: Text('Pizza tab'),)
    );
  }
}