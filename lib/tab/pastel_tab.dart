import 'package:flutter/material.dart';

class pastelTab extends StatelessWidget {
  const pastelTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
    child: const 
    Center(child: Text('Pastel tab'),)
    );
  }
}