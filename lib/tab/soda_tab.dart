import 'package:flutter/material.dart';

class sodaTab extends StatelessWidget {
  const sodaTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
    child: const 
    Center(child: Text('Soda tab'),)
    );
  }
}