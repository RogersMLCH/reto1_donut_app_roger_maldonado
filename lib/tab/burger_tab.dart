import 'package:flutter/material.dart';

class burgerTab extends StatelessWidget {
  const burgerTab({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
    child: const 
    Center(child: Text('Burger tab'),)
    );
  }
}